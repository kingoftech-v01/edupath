package com.edupath.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color palette for the EduPath Android application.
 *
 * This file defines all colors used throughout the app,
 * organized into categories for easy maintenance.
 */

// ==================== Material Default Colors ====================

/** Light purple for dark theme primary elements. */
val Purple80 = Color(0xFFD0BCFF)

/** Light purple-grey for dark theme secondary elements. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Light pink for dark theme tertiary elements. */
val Pink80 = Color(0xFFEFB8C8)

/** Dark purple for light theme primary elements. */
val Purple40 = Color(0xFF6650a4)

/** Dark purple-grey for light theme secondary elements. */
val PurpleGrey40 = Color(0xFF625b71)

/** Dark pink for light theme tertiary elements. */
val Pink40 = Color(0xFF7D5260)

// ==================== EduPath Brand Colors ====================

/** Primary brand violet - medium intensity. */
val Violet600 = Color(0xFF8B5CF6)

/** Primary brand violet - high intensity. */
val Violet700 = Color(0xFF7C3AED)

/** Primary brand violet - highest intensity. */
val Violet800 = Color(0xFF6D28D9)

// ==================== Dark Theme Colors ====================

/** Background color for dark theme. */
val DarkBackground = Color(0xFF1A1A2E)

/** Surface color for dark theme cards and dialogs. */
val DarkSurface = Color(0xFF16213E)

/** Variant surface color for dark theme. */
val DarkSurfaceVariant = Color(0xFF0F3460)

// ==================== Light Theme Colors ====================

/** Background color for light theme. */
val LightBackground = Color(0xFFFFFBFE)

/** Surface color for light theme cards and dialogs. */
val LightSurface = Color(0xFFFFFFFF)

// ==================== Text Colors ====================

/** Primary text color for dark backgrounds. */
val TextPrimary = Color(0xFFEAEAEA)

/** Secondary/muted text color. */
val TextSecondary = Color(0xFF9CA3AF)

/** Primary text color for light backgrounds. */
val TextDark = Color(0xFF1F2937)

// ==================== Status Colors ====================

/** Success/positive action color (green). */
val Success = Color(0xFF10B981)

/** Error/destructive action color (red). */
val Error = Color(0xFFEF4444)

/** Warning/caution color (orange). */
val Warning = Color(0xFFF59E0B)

/** Informational color (blue). */
val Info = Color(0xFF3B82F6)
